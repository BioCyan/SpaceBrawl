package application.control;

import application.model.GameSettings;
import application.control.GameControl;
import application.control.SettingsMenuControl;
/**
 * The first controller called by main
 * 
 * This will create a GameSettings object and set it's defaults then pass it to the 
 * GameControl object when the start button is press. Should also handle a button press to open
 * the settingsMenu view. 
 *
 */
public class MainMenuControl {
	private GameSettings settings;
	
}
